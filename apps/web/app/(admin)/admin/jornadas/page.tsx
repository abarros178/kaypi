import { Proximamente } from '../_components/proximamente';

export default function JornadasPage() {
  return (
    <Proximamente
      titulo="Jornadas"
      descripcion="Horarios por oficina: fija (entrada/salida + descanso) o flexible (solo máximos). Disparan retrasos y horas extra."
    />
  );
}
