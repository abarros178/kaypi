import { Proximamente } from '../_components/proximamente';

export default function PoliticasPage() {
  return (
    <Proximamente
      titulo="Políticas de check-in"
      descripcion="Por oficina: canales habilitados + nivel requerido (login / geo / facial) + enforcement (FLAG o BLOCK) + fallback."
    />
  );
}
