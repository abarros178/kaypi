import { Proximamente } from '../_components/proximamente';

export default function EmpleadosPage() {
  return (
    <Proximamente
      titulo="Empleados"
      descripcion="Alta y edición de empleados, asignación a oficina y jornada, y rol (ADMIN / MANAGER / EMPLEADO)."
    />
  );
}
